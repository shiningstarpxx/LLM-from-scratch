# Lecture 06: GPU Kernels & Performance Optimization - 教学大纲

## 📋 课程信息

**主题**: GPU Kernels & Performance Optimization  
**目标**: 从理论到实践，掌握GPU内核编写和性能优化  
**前置要求**: Lecture 05 (GPU Architecture)  
**预计时长**: 2-3周  
**学习深度**: ⭐⭐⭐⭐⭐ 专家级

---

## 🎯 课程目标

### 核心目标

1. **掌握Benchmarking和Profiling**
   - 理解Benchmarking vs Profiling的区别
   - 学会正确benchmark代码
   - 学会使用PyTorch Profiler
   - 理解性能瓶颈识别

2. **掌握CUDA内核编写**
   - 理解CUDA编程模型
   - 学会编写简单的CUDA内核
   - 理解Thread/Block/Grid的使用
   - 理解Shared Memory的使用

3. **掌握Triton内核编写**
   - 理解Triton的优势和特性
   - 学会编写Triton内核
   - 理解与CUDA的区别
   - 理解PTX代码生成

4. **理解Kernel Fusion**
   - 理解为什么需要fusion
   - 理解fusion的性能优势
   - 理解自动vs手动fusion
   - 理解torch.compile的作用

5. **实践复杂操作**
   - Softmax实现（行级聚合）
   - MatMul实现（复杂聚合）
   - 理解Tiling策略

---

## 📚 课程结构

### Part 1: GPU架构回顾与性能分析基础

#### 1.1 GPU架构回顾

**内容**:
- GPU硬件结构回顾
  - Streaming Multiprocessors (SMs)
  - 内存层次：DRAM、L2 Cache、L1 Cache
  - A100规格：108 SMs, 80GB DRAM, 40MB L2, 192KB L1/SM
- 执行模型回顾
  - Thread、Thread Block、Grid
  - Shared Memory的作用
  - Wave quantization和Occupancy
- Arithmetic Intensity回顾
  - FLOPs / Bytes
  - Compute-bound vs Memory-bound

**学习目标**:
- [ ] 巩固GPU硬件基础
- [ ] 理解执行模型
- [ ] 理解Arithmetic Intensity

**实践**:
- 查看实际GPU规格
- 理解硬件与执行模型的交互

---

#### 1.2 Benchmarking基础

**内容**:
- Benchmarking的定义
  - 测量整体执行时间
  - 不提供时间分布信息
- Benchmarking的用途
  - 比较不同实现
  - 理解性能缩放（scaling）
- Benchmarking实践
  - 如何正确benchmark
  - Warmup的重要性
  - CUDA同步的重要性
  - 多次运行取平均值
- 性能缩放分析
  - 矩阵乘法缩放
  - MLP缩放（steps、layers、batch_size、dim）

**学习目标**:
- [ ] 理解Benchmarking vs Profiling
- [ ] 学会正确benchmark代码
- [ ] 理解性能缩放规律

**实践**:
- 实现benchmark函数
- 分析矩阵乘法性能缩放
- 分析MLP性能缩放

**关键代码**:
```python
def benchmark(description: str, run: Callable, 
              num_warmups: int = 1, num_trials: int = 3):
    # Warmup
    for _ in range(num_warmups):
        run()
    if torch.cuda.is_available():
        torch.cuda.synchronize()
    
    # Time it
    times = []
    for trial in range(num_trials):
        start_time = time.time()
        run()
        if torch.cuda.is_available():
            torch.cuda.synchronize()
        end_time = time.time()
        times.append((end_time - start_time) * 1000)
    
    return mean(times)
```

---

#### 1.3 Profiling基础

**内容**:
- Profiling的定义
  - 分析时间分布
  - 理解内部调用
- PyTorch Profiler
  - 如何使用
  - 查看CUDA kernel调用
  - 理解kernel名称含义
  - Stack trace可视化
- Profiling实践
  - 基本操作（add、matmul）
  - 复合操作（cdist、gelu、softmax）
  - MLP profiling
  - Flame graph可视化

**学习目标**:
- [ ] 理解Profiling的作用
- [ ] 学会使用PyTorch Profiler
- [ ] 理解CUDA kernel调用
- [ ] 能识别性能瓶颈

**实践**:
- Profile基本操作
- Profile复合操作
- Profile MLP
- 分析性能瓶颈

**关键代码**:
```python
def profile(description: str, run: Callable, 
            num_warmups: int = 1, with_stack: bool = False):
    # Warmup
    for _ in range(num_warmups):
        run()
    if torch.cuda.is_available():
        torch.cuda.synchronize()
    
    # Profile
    with torch.profiler.profile(
            activities=[ProfilerActivity.CPU, ProfilerActivity.CUDA],
            with_stack=with_stack,
            experimental_config=torch._C._profiler._ExperimentalConfig(verbose=True)) as prof:
        run()
        if torch.cuda.is_available():
            torch.cuda.synchronize()
    
    # Print table
    table = prof.key_averages().table(sort_by="cuda_time_total",
                                      max_name_column_width=80,
                                      row_limit=10)
    return table
```

**关键洞察**:
- CUDA kernel名称包含实现信息
  - 例如：`cutlass_80_simt_sgemm_256x128_8x4_nn_align1`
  - `cutlass`: NVIDIA的CUDA线性代数库
  - `256x128`: tile size
- 不同tensor维度会调用不同的kernel

---

### Part 2: Kernel Fusion动机

#### 2.1 Fusion的动机

**内容**:
- Warehouse vs Factory类比
  - DRAM = Warehouse（大、慢）
  - SRAM = Factory（小、快）
- 多操作的问题
  - 每个操作：读→计算→写
  - 多次DRAM访问
- Fusion的解决方案
  - 融合操作：只读一次、只写一次
  - 减少DRAM访问
  - 性能提升

**学习目标**:
- [ ] 理解Fusion的动机
- [ ] 理解Warehouse/Factory类比
- [ ] 理解Fusion的性能优势

**实践**:
- 对比fused vs unfused实现
- 分析性能差异

---

#### 2.2 GeLU Fusion示例

**内容**:
- GeLU函数
  - PyTorch实现（fused）
  - 手动实现（unfused）
- 性能对比
  - Benchmark对比
  - Profile对比
- 关键洞察
  - PyTorch只调用一个kernel
  - 手动实现调用多个kernel

**学习目标**:
- [ ] 理解Fusion的实际效果
- [ ] 理解为什么PyTorch实现更快

**实践**:
- 实现manual_gelu
- 对比pytorch_gelu和manual_gelu
- Benchmark和Profile对比

**关键代码**:
```python
def pytorch_gelu(x: torch.Tensor):
    return torch.nn.functional.gelu(x, approximate="tanh")

def manual_gelu(x: torch.Tensor):
    return 0.5 * x * (1 + torch.tanh(0.79788456 * (x + 0.044715 * x * x * x)))
```

---

### Part 3: CUDA内核编写

#### 3.1 CUDA编程模型

**内容**:
- CUDA基础
  - C/C++扩展
  - GPU管理API
- 执行模型
  - Grid: 线程块集合
  - Thread Block: 线程集合
  - Thread: 单个执行单元
- 索引计算
  - blockIdx: 块索引
  - threadIdx: 线程索引
  - 全局索引计算

**学习目标**:
- [ ] 理解CUDA编程模型
- [ ] 理解Grid/Block/Thread
- [ ] 理解索引计算

**实践**:
- 理解CUDA代码结构
- 理解索引计算

---

#### 3.2 CUDA GeLU实现

**内容**:
- CUDA GeLU实现
  - 内核函数编写
  - 内存访问模式
  - 边界处理
- 编译和绑定
  - load_inline使用
  - C++绑定
- 性能分析
  - 正确性验证
  - Benchmark对比
  - Profile分析

**学习目标**:
- [ ] 学会编写CUDA内核
- [ ] 理解CUDA代码编译
- [ ] 理解性能优化

**实践**:
- 编写CUDA GeLU
- 编译和测试
- 性能对比

**关键代码** (gelu.cu):
```cuda
__global__ void gelu_kernel(float* x, float* y, int n) {
    int idx = blockIdx.x * blockDim.x + threadIdx.x;
    if (idx < n) {
        float val = x[idx];
        float a = 0.79788456f * (val + 0.044715f * val * val * val);
        float exp_val = expf(2.0f * a);
        float tanh_val = (exp_val - 1.0f) / (exp_val + 1.0f);
        y[idx] = 0.5f * val * (1.0f + tanh_val);
    }
}
```

**关键洞察**:
- Elementwise操作在CUDA中相对简单
- 复杂操作（matmul、softmax）需要Shared Memory管理
- CUDA实现可能不如PyTorch优化版本快

---

### Part 4: Triton内核编写

#### 4.1 Triton介绍

**内容**:
- Triton背景
  - OpenAI 2021年开发
  - 让GPU编程更易访问
- Triton特性
  - Python-like语法
  - 基于Block而非Thread
  - 自动优化
- Triton vs CUDA
  - Memory coalescing: 自动 vs 手动
  - Shared memory管理: 自动 vs 手动
  - SM内调度: 自动 vs 手动
  - SM间调度: 手动 vs 手动

**学习目标**:
- [ ] 理解Triton的优势
- [ ] 理解Triton vs CUDA
- [ ] 理解自动优化的价值

**实践**:
- 理解Triton语法
- 理解Block-based编程

---

#### 4.2 Triton GeLU实现

**内容**:
- Triton GeLU实现
  - Block-based编程
  - 内存访问模式
  - 边界处理
- PTX代码分析
  - PTX是什么
  - 如何查看PTX
  - PTX代码理解
- 性能分析
  - 正确性验证
  - Benchmark对比
  - Profile分析

**学习目标**:
- [ ] 学会编写Triton内核
- [ ] 理解PTX代码
- [ ] 理解性能优化

**实践**:
- 编写Triton GeLU
- 查看PTX代码
- 性能对比

**关键代码**:
```python
@triton.jit
def triton_gelu_kernel(x_ptr, y_ptr, num_elements, BLOCK_SIZE: tl.constexpr):
    pid = tl.program_id(axis=0)
    block_start = pid * BLOCK_SIZE
    offsets = block_start + tl.arange(0, BLOCK_SIZE)
    mask = offsets < num_elements
    
    x = tl.load(x_ptr + offsets, mask=mask)
    a = 0.79788456 * (x + 0.044715 * x * x * x)
    exp = tl.exp(2 * a)
    tanh = (exp - 1) / (exp + 1)
    y = 0.5 * x * (1 + tanh)
    
    tl.store(y_ptr + offsets, y, mask=mask)
```

**关键洞察**:
- Triton基于Block，允许编译器做更多优化（如thread coarsening）
- PTX代码显示一个thread处理8个元素（thread coarsening）
- Triton实现可能接近PyTorch优化版本

---

#### 4.3 PTX代码理解

**内容**:
- PTX基础
  - Parallel Thread Execution
  - GPU汇编语言
- PTX指令
  - `ld.global.*`: 从全局内存读取
  - `st.global.*`: 写入全局内存
  - `%ctaid.x`: block index
  - `%tid.x`: thread index
  - `%f*`: 浮点寄存器
  - `%r*`: 整数寄存器
- Thread Coarsening
  - 一个thread处理多个元素
  - 性能优化技术

**学习目标**:
- [ ] 理解PTX代码
- [ ] 理解GPU指令
- [ ] 理解Thread Coarsening

**实践**:
- 查看PTX代码
- 理解指令含义
- 理解优化技术

---

### Part 5: PyTorch编译

#### 5.1 torch.compile

**内容**:
- torch.compile介绍
  - PyTorch 2.0特性
  - 自动编译优化
- 5种实现方式
  1. Manual (Python)
  2. PyTorch (优化)
  3. CUDA (手动)
  4. Triton (手动)
  5. Compiled (torch.compile)
- 性能对比
  - Benchmark对比
  - Profile对比

**学习目标**:
- [ ] 理解torch.compile
- [ ] 理解自动编译的优势
- [ ] 理解不同实现方式的权衡

**实践**:
- 使用torch.compile
- 性能对比
- Profile分析

**关键代码**:
```python
compiled_gelu = torch.compile(manual_gelu)
```

**关键洞察**:
- torch.compile可以自动优化Python代码
- 自动编译器会越来越好
- 不需要手动编写CUDA/Triton代码

---

### Part 6: 复杂操作实现

#### 6.1 Softmax实现

**内容**:
- Softmax操作
  - 行级归一化
  - 用于Attention和概率生成
- 手动实现
  - 读取/写入分析
  - 5MN + M reads, 3MN + 2M writes
  - 理论上应该只有MN reads, MN writes（4x加速！）
- Triton实现
  - Block-based实现
  - 行级处理
  - 内存访问优化
- 性能对比
  - Manual vs Compiled vs PyTorch vs Triton
  - Profile分析
  - PTX代码分析

**学习目标**:
- [ ] 理解Softmax操作
- [ ] 理解聚合操作的挑战
- [ ] 学会实现Softmax
- [ ] 理解内存访问优化

**实践**:
- 实现manual_softmax
- 实现triton_softmax
- 性能对比
- Profile分析

**关键代码**:
```python
def manual_softmax(x: torch.Tensor):
    M, N = x.shape
    x_max = x.max(dim=1)[0]  # MN reads, M writes
    x = x - x_max[:, None]   # MN + M reads, MN writes
    numerator = torch.exp(x) # MN reads, MN writes
    denominator = numerator.sum(dim=1)  # MN reads, M writes
    y = numerator / denominator[:, None]  # MN reads, MN writes
    # Total: 5MN + M reads, 3MN + 2M writes
    return y

@triton.jit
def triton_softmax_kernel(x_ptr, y_ptr, x_row_stride, y_row_stride, 
                          num_cols, BLOCK_SIZE: tl.constexpr):
    row_idx = tl.program_id(0)
    col_offsets = tl.arange(0, BLOCK_SIZE)
    
    x_start_ptr = x_ptr + row_idx * x_row_stride
    x_ptrs = x_start_ptr + col_offsets
    x_row = tl.load(x_ptrs, mask=col_offsets < num_cols, other=float("-inf"))
    
    x_row = x_row - tl.max(x_row, axis=0)
    numerator = tl.exp(x_row)
    denominator = tl.sum(numerator, axis=0)
    y_row = numerator / denominator
    
    y_start_ptr = y_ptr + row_idx * y_row_stride
    y_ptrs = y_start_ptr + col_offsets
    tl.store(y_ptrs, y_row, mask=col_offsets < num_cols)
```

**关键洞察**:
- 聚合操作需要读取多个值
- 手动实现有大量冗余内存访问
- Triton实现可以优化内存访问

---

#### 6.2 MatMul实现（可选）

**内容**:
- MatMul操作
  - 最优化算法
  - CUDA实现复杂
- Tiling策略
  - 利用Shared Memory
  - 分块处理
  - 减少内存访问
- L2 Cache利用
  - 块处理顺序
  - 最小化读取
- Fusion应用
  - MatMul + GeLU fusion
  - 性能提升

**学习目标**:
- [ ] 理解MatMul的复杂性
- [ ] 理解Tiling策略
- [ ] 理解Fusion应用

**实践**:
- 理解Tiling策略
- 理解Fusion应用

**关键洞察**:
- MatMul是最优化的算法
- Tiling利用Shared Memory
- Fusion可以进一步提升性能

---

## 🎯 核心原则

### 1. 组织计算以最小化读写

```python
关键原则:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 减少DRAM访问
- 利用Shared Memory
- 数据复用
- Kernel Fusion
```

### 2. Kernel Fusion

```python
Fusion类比:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Warehouse (DRAM): 大、慢
Factory (SRAM): 小、快

Fusion = 在Factory中完成所有操作
→ 只读一次、只写一次
→ 性能提升
```

### 3. Tiling策略

```python
Tiling策略:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 分块处理
- 利用Shared Memory
- 减少内存访问
- 数据复用
```

---

## 📊 5种实现方式总结

```python
实现方式对比:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Manual (Python)
   - 易写
   - 慢（多个kernel）
   - 不优化

2. PyTorch (优化)
   - 易用
   - 快（fused kernel）
   - 自动优化

3. CUDA (手动)
   - 难写
   - 快（完全控制）
   - 需要手动优化

4. Triton (手动)
   - 中等难度
   - 快（自动优化）
   - Block-based

5. Compiled (torch.compile)
   - 易写
   - 快（自动编译）
   - 未来方向
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 🔗 与Lecture 05的连接

### 理论到实践

```python
Lecture 05 (理论):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- GPU硬件基础
- 内存层次
- Arithmetic Intensity
- Tiling和Fusion理论
- Tensor Cores原理

→ Lecture 06 (实践):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 实际编写CUDA内核
- 实际编写Triton内核
- 实际Benchmark和Profile
- 实际优化性能
- 实际应用Tiling和Fusion
```

---

## 📈 学习检查清单

### 基础理解 (60分)

- [ ] 理解Benchmarking vs Profiling
- [ ] 能正确benchmark代码
- [ ] 能使用PyTorch Profiler
- [ ] 理解CUDA基本结构
- [ ] 理解Triton基本语法

### 进阶理解 (80分)

- [ ] 能编写简单的CUDA内核
- [ ] 能编写简单的Triton内核
- [ ] 能识别性能瓶颈
- [ ] 能实现基本的fusion
- [ ] 能理解PTX代码

### 专家理解 (100分)

- [ ] 能优化CUDA内核性能
- [ ] 能优化Triton内核性能
- [ ] 深刻理解性能瓶颈根源
- [ ] 能设计高效的fusion策略
- [ ] 能实现复杂操作（Softmax、MatMul）
- [ ] 完成跨Lecture 05-06整合

---

## 📚 延伸阅读

### 推荐资源

1. **Horace He's Blog Post**
   - https://horace.io/brrr_intro.html
   - Warehouse/Factory类比

2. **CUDA MODE Lectures**
   - Lecture 1: How to profile CUDA kernels
   - Lecture 2-4: CUDA基础
   - Lecture 8: CUDA性能检查清单

3. **HetSys Course**
   - Lecture 1-5: GPU编程和性能

4. **Triton资源**
   - Triton Paper
   - Triton Tutorials
   - Triton Documentation

5. **其他资源**
   - GPU Puzzles
   - PyTorch 2.0 Acceleration
   - NVIDIA Deep Learning Performance Guide

---

## 🎯 课程总结

### 核心要点

```python
1. Benchmarking vs Profiling
   - Benchmarking: 整体时间
   - Profiling: 时间分布

2. 5种实现方式
   - Manual, PyTorch, CUDA, Triton, Compiled

3. 关键原则
   - 最小化读写
   - Kernel Fusion
   - Tiling策略

4. 自动编译器
   - Triton, torch.compile
   - 会越来越好
```

### 关键洞察

> **编程模型和硬件之间的gap → 性能神秘**  
> **Benchmarking理解缩放**  
> **Profiling理解内部（到底层kernel）**  
> **看PTX理解CUDA kernel内部**

---

**教学大纲创建日期**: 2025-12-14  
**覆盖内容**: Lecture 06全部内容  
**学习深度**: ⭐⭐⭐⭐⭐ 专家级  
**状态**: 完整大纲 ✅
