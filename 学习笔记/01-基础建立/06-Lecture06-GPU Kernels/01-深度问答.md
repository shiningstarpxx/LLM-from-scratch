# Lecture 06: GPU Kernels & Performance Optimization - 深度问答

## 📋 问题设计说明

**设计原则**:
- 继承Lecture 01-05的苏格拉底式风格
- 从"为什么"开始，层层深入
- 连接已学内容（Lecture 05）
- 理论+实践结合

**问题层次**:
- 第1层: 基础概念理解
- 第2层: 机制深入探索
- 第3层: 系统性思考
- 第4层: 实践应用

---

## Part 1: Benchmarking & Profiling (Q1-Q6)

### Q1: Benchmarking和Profiling有什么区别？

**引导思考**:
- 两者都测量性能，但有什么不同？
- 什么时候用Benchmarking？什么时候用Profiling？
- 它们提供的信息有什么不同？

**核心概念**:
```python
Benchmarking:
- 测量整体执行时间
- 不提供时间分布信息
- 用途: 比较不同实现、理解性能缩放

Profiling:
- 分析时间分布
- 理解内部调用
- 用途: 识别性能瓶颈、理解内部机制
```

**深入方向**:
- 如何正确benchmark代码？
- 如何使用PyTorch Profiler？
- 如何识别性能瓶颈？

**连接Lecture 05**:
- Lecture 05学了Arithmetic Intensity和Roofline Model
- Benchmarking可以验证理论分析
- Profiling可以理解实际执行

---

### Q2: 如何正确benchmark代码？

**引导思考**:
- 为什么需要warmup？
- 为什么需要CUDA同步？
- 为什么需要多次运行？
- 如何避免测量误差？

**核心概念**:
```python
正确benchmark的步骤:
1. Warmup: 第一次运行可能慢（编译、缓存）
2. CUDA同步: 等待GPU完成（重要！）
3. 多次运行: 捕获方差
4. 取平均值: 获得稳定结果
```

**深入方向**:
- 为什么第一次运行慢？
- CUDA异步执行的影响？
- 如何选择warmup和trial次数？

**实践代码**:
```python
def benchmark(description: str, run: Callable, 
              num_warmups: int = 1, num_trials: int = 3):
    # Warmup
    for _ in range(num_warmups):
        run()
    if torch.cuda.is_available():
        torch.cuda.synchronize()  # 重要！
    
    # Time it
    times = []
    for trial in range(num_trials):
        start_time = time.time()
        run()
        if torch.cuda.is_available():
            torch.cuda.synchronize()  # 重要！
        end_time = time.time()
        times.append((end_time - start_time) * 1000)
    
    return mean(times)
```

---

### Q3: 如何使用PyTorch Profiler？

**引导思考**:
- Profiler能告诉我们什么？
- 如何查看CUDA kernel调用？
- 如何理解kernel名称？
- 如何可视化stack trace？

**核心概念**:
```python
PyTorch Profiler:
- 查看CPU和CUDA活动
- 查看kernel调用
- 查看时间分布
- Stack trace可视化（flame graph）
```

**深入方向**:
- CUDA kernel名称的含义？
- 如何识别性能瓶颈？
- 如何优化？

**实践代码**:
```python
def profile(description: str, run: Callable, 
            num_warmups: int = 1, with_stack: bool = False):
    # Warmup
    for _ in range(num_warmups):
        run()
    if torch.cuda.is_available():
        torch.cuda.synchronize()
    
    # Profile
    with torch.profiler.profile(
            activities=[ProfilerActivity.CPU, ProfilerActivity.CUDA],
            with_stack=with_stack,
            experimental_config=torch._C._profiler._ExperimentalConfig(verbose=True)) as prof:
        run()
        if torch.cuda.is_available():
            torch.cuda.synchronize()
    
    # Print table
    table = prof.key_averages().table(sort_by="cuda_time_total",
                                      max_name_column_width=80,
                                      row_limit=10)
    return table
```

**关键洞察**:
- CUDA kernel名称包含实现信息
  - 例如：`cutlass_80_simt_sgemm_256x128_8x4_nn_align1`
  - `cutlass`: NVIDIA的CUDA线性代数库
  - `256x128`: tile size
- 不同tensor维度会调用不同的kernel

---

### Q4: 性能缩放（Scaling）有什么规律？

**引导思考**:
- 矩阵乘法如何随维度缩放？
- MLP如何随layers、batch_size、dim缩放？
- 为什么不是线性的？
- 如何理解性能瓶颈？

**核心概念**:
```python
性能缩放分析:
- 矩阵乘法: 维度³ → 时间？
- MLP layers: 线性缩放？
- MLP batch_size: 线性缩放？
- MLP dim: 如何缩放？

关键: 理解瓶颈在哪里
- Compute-bound: 线性缩放
- Memory-bound: 可能非线性
```

**深入方向**:
- 如何分析缩放规律？
- 如何识别瓶颈？
- 如何优化？

**连接Lecture 05**:
- Arithmetic Intensity分析
- Compute-bound vs Memory-bound
- Roofline Model

---

### Q5: 为什么需要Profiling？

**引导思考**:
- Benchmarking只告诉我们整体时间
- 但不知道时间花在哪里
- 如何找到性能瓶颈？
- 如何优化？

**核心概念**:
```python
Profiling的价值:
1. 理解内部调用
   - 哪些kernel被调用？
   - 每个kernel花多少时间？

2. 识别性能瓶颈
   - 哪个kernel最慢？
   - 为什么慢？

3. 优化方向
   - 如何优化？
   - 预期加速比？
```

**深入方向**:
- 如何分析profile结果？
- 如何识别优化机会？
- 如何验证优化效果？

---

### Q6: 如何理解CUDA kernel名称？

**引导思考**:
- kernel名称包含什么信息？
- 如何解读？
- 不同名称意味着什么？

**核心概念**:
```python
CUDA kernel名称解读:
cutlass_80_simt_sgemm_256x128_8x4_nn_align1

- cutlass: NVIDIA的CUDA线性代数库
- 80: 架构（A100）
- simt: SIMT执行模式
- sgemm: 单精度矩阵乘法
- 256x128: tile size
- 8x4: warp tile size
- nn: 无转置
- align1: 对齐方式
```

**深入方向**:
- 如何理解tile size？
- 如何理解warp tile size？
- 如何选择最优kernel？

---

## Part 2: Kernel Fusion (Q7-Q9)

### Q7: 为什么需要Kernel Fusion？

**引导思考**:
- 每个操作都需要读→计算→写
- 多次操作意味着多次DRAM访问
- 如何减少DRAM访问？
- Warehouse/Factory类比？

**核心概念**:
```python
Fusion的动机:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DRAM (Warehouse): 大、慢
SRAM (Factory): 小、快

多操作问题:
- 每个操作: 读DRAM → 计算 → 写DRAM
- 多次DRAM访问 → 慢

Fusion解决方案:
- 融合操作: 只读一次DRAM → 在SRAM中计算 → 只写一次DRAM
- 减少DRAM访问 → 快
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**深入方向**:
- 如何实现fusion？
- 性能提升多少？
- 什么时候需要fusion？

**连接Lecture 05**:
- Lecture 05学了Fusion理论
- 现在看实际效果

---

### Q8: GeLU的fused vs unfused实现有什么区别？

**引导思考**:
- PyTorch的GeLU实现是什么？
- 手动的GeLU实现是什么？
- 性能有什么区别？
- 为什么？

**核心概念**:
```python
PyTorch GeLU (fused):
- 一个kernel
- 一次DRAM访问
- 快

Manual GeLU (unfused):
- 多个kernel
- 多次DRAM访问
- 慢
```

**实践代码**:
```python
def pytorch_gelu(x: torch.Tensor):
    return torch.nn.functional.gelu(x, approximate="tanh")

def manual_gelu(x: torch.Tensor):
    return 0.5 * x * (1 + torch.tanh(0.79788456 * (x + 0.044715 * x * x * x)))
```

**深入方向**:
- 如何验证正确性？
- 如何benchmark？
- 如何profile？

---

### Q9: 如何理解Fusion的性能提升？

**引导思考**:
- Fusion减少了什么？
- 性能提升多少？
- 为什么不是无限的？
- 有什么限制？

**核心概念**:
```python
Fusion性能提升:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
减少DRAM访问:
- Unfused: N次操作 → N次读 + N次写
- Fused: N次操作 → 1次读 + 1次写
- 减少: (N-1)次读 + (N-1)次写

性能提升:
- 取决于DRAM访问时间占比
- 通常: 1.5-2倍
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**深入方向**:
- 如何量化性能提升？
- 什么情况下fusion最有效？
- 有什么权衡？

---

## Part 3: CUDA内核编写 (Q10-Q13)

### Q10: CUDA编程模型是什么？

**引导思考**:
- CUDA如何组织并行计算？
- Grid、Block、Thread是什么关系？
- 如何计算全局索引？
- 如何映射到硬件？

**核心概念**:
```python
CUDA执行模型:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Grid: 线程块集合
  └── Thread Block: 线程集合
      └── Thread: 单个执行单元

索引计算:
- blockIdx: 块索引
- threadIdx: 线程索引
- 全局索引 = blockIdx.x * blockDim.x + threadIdx.x
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**深入方向**:
- 如何选择block size？
- 如何选择grid size？
- 如何映射到SM？

**连接Lecture 05**:
- Lecture 05学了Thread/Block/Grid
- 现在实际使用

---

### Q11: 如何编写CUDA GeLU内核？

**引导思考**:
- CUDA内核函数的结构？
- 如何访问内存？
- 如何处理边界？
- 如何编译和绑定？

**核心概念**:
```python
CUDA内核结构:
1. 内核函数: __global__ void kernel(...)
2. 索引计算: int idx = blockIdx.x * blockDim.x + threadIdx.x
3. 边界检查: if (idx < n)
4. 计算: 实际计算
5. 写入: 写入结果
```

**实践代码** (gelu.cu):
```cuda
__global__ void gelu_kernel(float* in, float* out, int num_elements) {
    int i = blockIdx.x * blockDim.x + threadIdx.x;
    if (i < num_elements) {
        out[i] = 0.5 * in[i] * (1.0 + tanh(0.79788456 * 
            (in[i] + 0.044715 * in[i] * in[i] * in[i])));
    }
}
```

**深入方向**:
- 如何编译CUDA代码？
- 如何绑定到Python？
- 如何测试正确性？

---

### Q12: CUDA实现为什么可能不如PyTorch快？

**引导思考**:
- 我们的CUDA实现是naive的
- PyTorch的实现是高度优化的
- 优化包括什么？
- 如何优化？

**核心概念**:
```python
PyTorch优化:
- 使用Tensor Cores
- 优化的内存访问模式
- 自动tiling
- 自动fusion

我们的CUDA实现:
- 简单的elementwise操作
- 没有特殊优化
- 可能不如PyTorch快
```

**深入方向**:
- 如何优化CUDA实现？
- 什么时候需要自己写CUDA？
- 什么时候用PyTorch？

---

### Q13: Elementwise操作 vs 复杂操作的区别？

**引导思考**:
- GeLU是elementwise操作
- Softmax、MatMul是复杂操作
- 有什么区别？
- 实现难度如何？

**核心概念**:
```python
Elementwise操作 (如GeLU):
- 每个元素独立计算
- 简单: 直接计算
- 不需要Shared Memory

复杂操作 (如Softmax、MatMul):
- 需要读取多个值
- 复杂: 需要Shared Memory管理
- 需要Tiling策略
```

**深入方向**:
- 如何实现复杂操作？
- 如何管理Shared Memory？
- 如何应用Tiling？

---

## Part 4: Triton内核编写 (Q14-Q18)

### Q14: Triton是什么？为什么需要Triton？

**引导思考**:
- CUDA编程有什么困难？
- Triton如何解决？
- Triton的优势是什么？
- 什么时候用Triton？

**核心概念**:
```python
Triton特性:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- Python-like语法
- 基于Block而非Thread
- 自动优化:
  - Memory coalescing: 自动
  - Shared memory管理: 自动
  - SM内调度: 自动
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

vs CUDA:
- CUDA: 手动优化
- Triton: 自动优化
```

**深入方向**:
- Triton如何工作？
- 如何编写Triton内核？
- 性能如何？

---

### Q15: 如何编写Triton GeLU内核？

**引导思考**:
- Triton语法是什么？
- 如何访问内存？
- 如何处理边界？
- 如何launch kernel？

**核心概念**:
```python
Triton内核结构:
1. 内核函数: @triton.jit def kernel(...)
2. Block-based: 基于block而非thread
3. 内存访问: tl.load, tl.store
4. 边界处理: mask
```

**实践代码**:
```python
@triton.jit
def triton_gelu_kernel(x_ptr, y_ptr, num_elements, BLOCK_SIZE: tl.constexpr):
    pid = tl.program_id(axis=0)
    block_start = pid * BLOCK_SIZE
    offsets = block_start + tl.arange(0, BLOCK_SIZE)
    mask = offsets < num_elements
    
    x = tl.load(x_ptr + offsets, mask=mask)
    # 计算GeLU
    y = 0.5 * x * (1 + tanh(...))
    tl.store(y_ptr + offsets, y, mask=mask)
```

**深入方向**:
- 如何理解Block-based编程？
- 如何选择BLOCK_SIZE？
- 如何launch kernel？

---

### Q16: PTX代码是什么？如何理解？

**引导思考**:
- PTX是什么？
- 如何查看PTX？
- PTX代码告诉我们什么？
- 如何理解PTX指令？

**核心概念**:
```python
PTX (Parallel Thread Execution):
- GPU汇编语言
- 中间表示
- 可以查看生成的指令

关键指令:
- ld.global.*: 从全局内存读取
- st.global.*: 写入全局内存
- %ctaid.x: block index
- %tid.x: thread index
- %f*: 浮点寄存器
- %r*: 整数寄存器
```

**深入方向**:
- 如何查看PTX？
- 如何理解PTX代码？
- Thread Coarsening是什么？

---

### Q17: Thread Coarsening是什么？

**引导思考**:
- PTX代码显示一个thread处理多个元素
- 这是为什么？
- 有什么好处？
- 如何实现？

**核心概念**:
```python
Thread Coarsening:
- 一个thread处理多个元素
- 减少thread数量
- 增加每个thread的工作量
- 性能优化技术

好处:
- 减少调度开销
- 更好的寄存器利用
- 更好的内存访问模式
```

**深入方向**:
- 如何实现Thread Coarsening？
- 什么时候有效？
- 有什么权衡？

---

### Q18: Triton vs CUDA的性能对比？

**引导思考**:
- Triton实现 vs CUDA实现
- 性能如何？
- 为什么？
- 什么时候用哪个？

**核心概念**:
```python
性能对比:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Triton GeLU:
- 接近PyTorch优化版本
- 自动优化
- 易写

CUDA GeLU:
- 可能不如PyTorch
- 手动优化
- 难写

关键: Triton编译器可以做更多优化
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**深入方向**:
- 如何选择实现方式？
- 什么时候用Triton？
- 什么时候用CUDA？

---

## Part 5: PyTorch编译 (Q19-Q20)

### Q19: torch.compile是什么？

**引导思考**:
- 我们学了5种实现方式
- torch.compile是第5种
- 它如何工作？
- 有什么优势？

**核心概念**:
```python
torch.compile:
- PyTorch 2.0特性
- 自动编译Python代码
- 自动优化
- 自动fusion

5种实现方式:
1. Manual (Python)
2. PyTorch (优化)
3. CUDA (手动)
4. Triton (手动)
5. Compiled (torch.compile)
```

**深入方向**:
- 如何使用torch.compile？
- 性能如何？
- 什么时候用？

---

### Q20: 自动编译器会越来越好？

**引导思考**:
- 手动优化 vs 自动优化
- 自动编译器的优势？
- 未来趋势？
- 什么时候需要手动优化？

**核心概念**:
```python
自动编译器优势:
- 易写: 写Python代码
- 自动优化: 编译器优化
- 可维护: 不需要维护CUDA代码

未来趋势:
- 自动编译器会越来越好
- 可能不需要手动写CUDA/Triton
- 但理解原理仍然重要
```

**深入方向**:
- 什么时候需要手动优化？
- 如何平衡易用性和性能？
- 未来会怎样？

---

## Part 6: 复杂操作实现 (Q21-Q24)

### Q21: Softmax操作的挑战是什么？

**引导思考**:
- Softmax需要做什么？
- 手动实现有什么问题？
- 内存访问如何？
- 如何优化？

**核心概念**:
```python
Softmax操作:
- 行级归一化
- 需要读取整行
- 需要聚合操作

手动实现问题:
- 5MN + M reads, 3MN + 2M writes
- 理论上应该只有MN reads, MN writes
- 4x加速潜力！
```

**深入方向**:
- 如何优化Softmax？
- 如何减少内存访问？
- Triton如何实现？

---

### Q22: 如何实现Triton Softmax？

**引导思考**:
- Softmax需要聚合
- 如何在Triton中实现？
- 如何管理内存？
- 如何优化？

**核心概念**:
```python
Triton Softmax:
- Block-based实现
- 每个block处理一行
- 在block内聚合
- 减少内存访问
```

**实践代码**:
```python
@triton.jit
def triton_softmax_kernel(x_ptr, y_ptr, x_row_stride, y_row_stride, 
                          num_cols, BLOCK_SIZE: tl.constexpr):
    row_idx = tl.program_id(0)
    col_offsets = tl.arange(0, BLOCK_SIZE)
    
    # 读取一行
    x_row = tl.load(...)
    
    # 计算softmax
    x_row = x_row - tl.max(x_row, axis=0)
    numerator = tl.exp(x_row)
    denominator = tl.sum(numerator, axis=0)
    y_row = numerator / denominator
    
    # 写入
    tl.store(...)
```

**深入方向**:
- 如何理解Block-based聚合？
- 如何优化？
- 性能如何？

---

### Q23: MatMul的Tiling策略是什么？

**引导思考**:
- MatMul需要什么？
- 为什么需要Tiling？
- 如何利用Shared Memory？
- 如何优化？

**核心概念**:
```python
MatMul Tiling:
- 分块处理
- 利用Shared Memory
- 减少内存访问
- 数据复用

关键思想:
- 加载tile到Shared Memory
- 在Shared Memory中计算
- 只写最终结果
```

**深入方向**:
- 如何实现Tiling？
- 如何选择tile size？
- 如何优化L2 Cache利用？

---

### Q24: 什么时候需要自己写Kernel？

**引导思考**:
- 5种实现方式
- 什么时候用哪个？
- 什么时候需要自己写？
- 权衡是什么？

**核心概念**:
```python
实现方式选择:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. PyTorch: 默认选择，易用
2. torch.compile: 自动优化，易用
3. Triton: 需要特定优化，中等难度
4. CUDA: 完全控制，难写

需要自己写的情况:
- PyTorch没有的优化
- 需要特定fusion
- 需要极致性能
- 研究新算法
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**深入方向**:
- 如何做决策？
- 如何平衡易用性和性能？
- 未来趋势？

---

## 🎯 核心原则总结

### 1. 组织计算以最小化读写

```python
关键原则:
- 减少DRAM访问
- 利用Shared Memory
- 数据复用
- Kernel Fusion
```

### 2. Kernel Fusion

```python
Fusion = 在SRAM中完成所有操作
→ 只读一次、只写一次
→ 性能提升
```

### 3. Tiling策略

```python
Tiling = 分块处理 + Shared Memory
→ 减少内存访问
→ 数据复用
```

---

## 🔗 与Lecture 05的连接

### 理论到实践

```python
Lecture 05 (理论):
- GPU硬件基础
- 内存层次
- Arithmetic Intensity
- Tiling和Fusion理论

→ Lecture 06 (实践):
- 实际编写CUDA内核
- 实际编写Triton内核
- 实际Benchmark和Profile
- 实际优化性能
```

---

**深度问答创建日期**: 2025-12-14  
**覆盖内容**: Lecture 06全部24个问题  
**学习深度**: ⭐⭐⭐⭐⭐ 专家级  
**状态**: 完整问题集 ✅

💬 **准备好了就告诉我: "让我们开始Q1的讨论"**
